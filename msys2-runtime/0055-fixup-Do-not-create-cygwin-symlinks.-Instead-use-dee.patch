From d1be301f6bf8b64430f5205ba46035924785429e Mon Sep 17 00:00:00 2001
From: Johannes Schindelin <johannes.schindelin@gmx.de>
Date: Mon, 27 Apr 2020 19:41:33 +0200
Subject: [PATCH 55/N] fixup! Do not create cygwin symlinks. Instead use deep
 copy of files/folders.

---
 winsup/cygwin/path.cc | 1 +
 1 file changed, 1 insertion(+)

diff --git a/winsup/cygwin/path.cc b/winsup/cygwin/path.cc
index 716e1fb..ea70af8 100644
--- a/winsup/cygwin/path.cc
+++ b/winsup/cygwin/path.cc
@@ -2216,6 +2216,7 @@ symlink_worker (const char *oldpath, path_conv &win32_newpath, bool isdevice)
                  to the directory of the destination */
              {
                 /* Determine the character position of the last path component */
+                const char *newpath = win32_newpath.get_posix();
                 int pos = strlen (newpath);
                 while (--pos >= 0)
                   if (isdirsep (newpath[pos]))
-- 
2.9.0

